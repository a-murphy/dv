language: none

build:
  ci:
    - echo 'CI Running'
    - export IMAGE_NAME=amurphy/samplepipelinesdv
    - ./build.sh

  post_ci:
    - ./push.sh

integrations:
  hub:
    - integrationName: "shipdeploy"
      type: docker
